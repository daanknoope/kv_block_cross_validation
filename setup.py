from setuptools import setup, find_packages

setup(name='kv_block_cross_validation',
      version='0.1',
      description='Cross validation for dependent datasets.',
      author='Daan Knoope',
      author_email='daan@knoope.dev',
      license='MIT',
      packages=find_packages(), install_requires=[],
      python_requires='>3.5'
      )
